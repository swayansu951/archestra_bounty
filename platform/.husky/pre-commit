cd platform
if ! pnpm commit:check; then
  echo ""
  echo "💡 Tip: Run 'pnpm lint:fix -- --unsafe' to auto-fix formatting issues."
  echo "💡 Tip: Run 'pnpm -r knip --fix' to auto-fix unused exports/files."
  echo ""
  exit 1
fi
